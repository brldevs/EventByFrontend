import Image from "next/image";
import Link from "next/link";

const Logo = (params) => {
  return (
    <Link href="/">
      {/* <a className="navbar-brand">
        <object data="../img/logo.svg" width={140} type="image/svg+xml" />
      </a>    */}{" "}
      EventBy
    </Link>
  );
};

export default Logo;
